[
  {
    "id": "i4h-workflow-setup-readme-quick-setup",
    "question": "Set up the i4h workflow on this machine and tell me if any host requirements are missing.",
    "expected_skill": "i4h-workflow-setup",
    "ground_truth": "This README Quick Run prompt routes to setup. The agent resolves the repo root, checks host prerequisites, runs ./setup.sh as needed, and reports missing requirements.",
    "expected_behavior": [
      "routes to i4h-workflow-setup",
      "checks host requirements such as uv, git, GPU driver, disk, and third-party checkout state",
      "runs ./setup.sh from the repo root for a full setup",
      "uses only the default setup, explicit all, or explicit full clean operations",
      "prepares the pinned sparse Isaac Sim skill catalog with the other third-party references",
      "runs live workflow discovery and lint --all",
      "reports missing requirements and verification results"
    ]
  }
]
